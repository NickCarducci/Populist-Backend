# IDWise Webhook Handler - Environment Variables
# Copy this file to .env and fill in your actual values

# Firebase Configuration
# For local development, download your service account JSON from Firebase Console:
# Project Settings → Service Accounts → Generate New Private Key
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
FIREBASE_PROJECT_ID=pop-u-list

# IDWise Configuration (optional - for additional validation)
IDWISE_CLIENT_KEY=your_idwise_client_key_here

# Server Configuration
PORT=3000
NODE_ENV=development

# Security (for production)
# Comma-separated list of allowed IDWise IP addresses
# Get these from IDWise support
ALLOWED_IPS=

# Optional: Webhook secret for signature verification
# (if IDWise provides webhook signing)
WEBHOOK_SECRET=
